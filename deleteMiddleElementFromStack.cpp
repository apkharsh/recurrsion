// Find the middle element of an stack using recurrsion

#include <bits/stdc++.h>
using namespace std;

stack<int> findMiddle(stack<int> &s){
    if(s.empty()) return s;
    int val = s.top();
    s.pop();

    if(current = size/2)

}